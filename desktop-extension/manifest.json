{
  "manifest_version": "0.3",
  "name": "intervals-mcp",
  "version": "1.0.0",
  "display_name": "intervals.icu — Triathlon Training",
  "description": "Connects Claude to your intervals.icu training data. Required for the Triathlon Training Insights skill. Powered by intervals-mcp-server by mvilanova (https://github.com/mvilanova/intervals-mcp-server).",
  "icon": "icon.png",
  "author": {
    "name": "intervals.icu Triathlon Coach"
  },

  "user_config": {
    "API_KEY": {
      "type": "string",
      "title": "intervals.icu API Key",
      "description": "Found at intervals.icu → Settings → API → API Key",
      "sensitive": true,
      "required": true
    },
    "ATHLETE_ID": {
      "type": "string",
      "title": "Athlete ID",
      "description": "Found at intervals.icu → Settings → API (e.g. i12345)",
      "required": true
    }
  },

  "server": {
    "type": "python",
    "entry_point": "server/run.py",
    "mcp_config": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/mvilanova/intervals-mcp-server.git",
        "python",
        "-m",
        "intervals_mcp_server.server"
      ],
      "env": {
        "API_KEY": "${user_config.API_KEY}",
        "ATHLETE_ID": "${user_config.ATHLETE_ID}"
      }
    }
  }
}
