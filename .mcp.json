{
  "mcpServers": {
    "intervals-mcp": {
      "type": "stdio",
      "command": "npm",
      "args": ["run", "stdio"],
      "cwd": "/path/to/intervals.icu-server",
      "env": {
        "API_KEY": "${INTERVALS_API_KEY}",
        "ATHLETE_ID": "${INTERVALS_ATHLETE_ID}"
      }
    }
  }
}
